"use client";

import { Button } from "@heroui/button";

const SubmitButton = () => {
  return (
    <div>
      <Button type="submit" color="primary">
        Speichern
      </Button>
    </div>
  );
};

export default SubmitButton;
